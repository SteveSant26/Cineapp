from tkinter import *

app = Tk()
app.geometry("900x800")

def nueva_ventana():
    nueva = Toplevel(app)
    nueva.geometry("800x2000")

Button(app, text = "Crear ventana", command = nueva_ventana).pack()

mainloop()